import { Injectable } from '@nestjs/common';

@Injectable()
export class CategoryService {
  create() {
  }

  findAll() {
  }

  findOne(id: number) {
  }

  update(id: number) {
  }

  delete(id: number) {
  }
}

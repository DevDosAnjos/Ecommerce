import { Injectable } from '@nestjs/common';

@Injectable()
export class ProductService {
  create() {
  }

  findAll() {
  }

  findOne(id: number) {
  }

  update(id: number) {
  }

  remove(id: number) {
  }
}
